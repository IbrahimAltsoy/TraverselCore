@if (User.Identity.IsAuthenticated)
{

    <div class="leave-comment-form mt-5" id="reply">
        <h4 class="side-title mb-2">Yorum Yap</h4>

        <form method="post" asp-action="AddComment" asp-controller="Comment">
            <input type="hidden" name="DestinationId" value="@ViewBag.DestinationId" />
            <input type="hidden" name="AppUserId" value="@ViewBag.v" />
            <input type="hidden" name="User" value="@ViewBag.userName" />

            <div class="form-group">
                <textarea name="Content" class="form-control" placeholder="Yorum yaz*" required=""
                      spellcheck="false"></textarea>
            </div>
            @* <div class="input-grids row">

        <div class="form-group col-lg-6">
        <input type="text" name="User" class="form-control" placeholder="Your Name*"
        required="">
        </div>
        <div class="form-group col-lg-6">
        <input type="text" name="DestinationId" class="form-control" placeholder="Email*" required="">
        </div>
        </div>*@

            <div class="submit text-right">
                <button type="submit" class="btn btn-style btn-primary">Yorum Ekle </button>
            </div>
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1min.js" type="text/javascript"></script>
}
else
{
    <br />
    <br />
    <br />
    <h3>Yorum yapabilmek için lütfen giriş yapınız.</h3>
    <div class="submit text-right">
        <a href="/Login/Login/" class="btn btn-style btn-primary">Giriş Yap</a>

    </div>
}